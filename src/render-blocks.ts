/**
 * render-blocks.ts — Compatibility shim.
 * The implementation moved to ./render-blocks/ as 12 sub-modules + a barrel.
 * Kept temporarily as a re-export so any straggling import of './render-blocks.js' still works.
 */
export * from './render-blocks/index.js'
