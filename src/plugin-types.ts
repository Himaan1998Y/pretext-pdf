/**
 * Plugin extension points for pretext-pdf.
 *
 * This module is a stub prepared in Batch B. The plugin contract
 * (PluginDefinition, MeasureHook, RenderHook, etc.) will be implemented
 * in Batch C alongside the plugin registry.
 *
 * @remarks
 * Import from `pretext-pdf/plugin-types` once the contract is finalized.
 *
 * @beta
 */

// Batch C will populate this module.
export {}
